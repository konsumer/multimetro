{
  "name": "multimetro",
  "version": "0.0.0",
  "private": true,
  "description": "A metronome that follows a song structure, with band sync, on one Cloudflare Worker.",
  "type": "module",
  "scripts": {
    "dev": "npx -y wrangler dev",
    "deploy": "npx -y wrangler deploy",
    "fmt": "npx -y prettier -w ."
  }
}
